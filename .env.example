# Sample .env for TrajectoryVisualizer — copy to `.env` and fill in.
# Used by scripts/step_labeler.py (per-step phase/action labels) and
# scripts/training_labeler.py (behavior/quality/value training labels).
# `.env` is gitignored; keep your real API key out of version control.
#
# NOTE: both labelers send trajectory content (assistant text, reasoning, tool
# I/O — which can contain proprietary code/secrets) to LABEL_BASE_URL. Point it
# at an endpoint you trust with that data.

# --- Required -------------------------------------------------------------

# OpenAI-compatible API base URL.
# Examples:
#   OpenAI:       https://api.openai.com/v1
#   OpenRouter:   https://openrouter.ai/api/v1
#   Anthropic:    https://api.anthropic.com
#   Self-hosted:  http://localhost:8000/v1
LABEL_BASE_URL=https://api.openai.com/v1

# API key for the provider above.
LABEL_API_KEY=sk-xxxxx-xxxxx

# Model identifier understood by the provider.
# Examples: gpt-4o-mini, gpt-4o, claude-3-5-sonnet-20241022, glm-4.6
LABEL_MODEL=gpt-5.4

# --- Optional -------------------------------------------------------------

# Provider flavor: "openai" (default, OpenAI-compatible chat/completions)
# or "anthropic" (native Anthropic messages API).
LABEL_PROVIDER=openai

# Sampling temperature (default 0.3). Lower → more deterministic labels.
LABEL_TEMPERATURE=0.3

# Max response tokens per step (default 1024).
LABEL_MAX_TOKENS=1024

# --- Optional: training_labeler.py per-dimension model overrides ----------
# training_labeler.py labels three dimensions; each can use a different model.
# If unset, they fall back to LABEL_MODEL above.
# BEHAVIOR_LABEL_MODEL=gpt-4o-mini
# QUALITY_LABEL_MODEL=gpt-4o
# VALUE_LABEL_MODEL=gpt-4o-mini
